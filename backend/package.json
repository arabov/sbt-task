{
  "name": "backend",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "prod": "NODE_ENV=production PORT=1111 node --experimental-modules src/index.mjs",
    "dev": "PORT=1110 nodemon --experimental-modules src/index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "morgan": "^1.9.1",
    "socket.io": "^2.2.0"
  }
}
